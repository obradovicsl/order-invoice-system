apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  type: ClusterIP 
  ports:
  - port: 5432 # Inside cluster
    targetPort: 5432 # PostgreSQL container
    # nodePort: 30432 
  selector:
    app: postgres